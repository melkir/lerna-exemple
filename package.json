{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run --parallel start",
    "diff": "lerna diff",
    "test": "lerna run test --since",
    "check:changes": "lerna changed --all --conventional-graduate",
    "bump:version": "lerna version --yes"
  },
  "devDependencies": {
    "lerna": "^3.19.0",
    "ts-node": "^8.5.4",
    "typescript": "^3.7.3"
  }
}
